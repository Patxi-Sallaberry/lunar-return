# Regenerated by `run_all` in a couple of minutes; not worth versioning.
results/traj_part*.mat
results/video/frames/
results/FAIL_*.txt
results/video/clips/concat_list.txt

# Figures, clips, the showreel, metrics.mat and metrics.json ARE committed:
# the README links to them and a reader should see the results without
# installing MATLAB.

# MATLAB scratch
*.asv
*.autosave
*.mex*
slprj/
codegen/
